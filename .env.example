# Copy to .env and fill in your MySQL connection details.
# The API will not start if the database is unreachable.

PORT=3000

DB_HOST=localhost
DB_PORT=3306
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=hims_db